# Padrón A4 — Python example

Prereq: TA from `afip-arca-wsaa` con `--service ws_sr_padron_a4`.

```bash
pip install -r requirements.txt
python padron_a4.py --ta ta-padron.xml --cuit-rep 20111111112 --cuit 30500001735
```
